A React application built with Material-UI (MUI) that allows users to create and save customer segments dynamically.

Each segment includes a name and one or more user/group schemas that can be selected and sent to a backend (or webhook URL).
